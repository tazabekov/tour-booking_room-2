# Tour Booking API Backend

Backend для платформы бронирования туров. Построен на FastAPI с SQLite базой данных.

## Технологии

- **FastAPI** - современный веб-фреймворк
- **SQLAlchemy 2.0** - async ORM
- **SQLite** - база данных
- **Pydantic v2** - валидация данных
- **Uvicorn** - ASGI сервер

## Функционал

### Туры (Tours)
- ✅ Список туров с пагинацией
- ✅ Фильтрация по стране
- ✅ Фильтрация по цене (min/max)
- ✅ Фильтрация по датам (start/end)
- ✅ Просмотр деталей тура

### Бронирование (Bookings)
- ✅ Создание бронирования
- ✅ Автоматический расчет стоимости
- ✅ Проверка доступных мест
- ✅ Просмотр бронирования по ID
- ✅ Получение всех бронирований по email

## Установка и запуск

### 1. Создать виртуальное окружение

**Windows:**
```bash
cd backend
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
cd backend
python3 -m venv venv
source venv/bin/activate
```

### 2. Установить зависимости

```bash
pip install -r requirements.txt
```

### 3. Создать файл .env

```bash
cp .env.example .env
```

### 4. Инициализировать базу данных

```bash
python init_db.py
```

Эта команда создаст:
- ✅ Таблицы базы данных
- ✅ 10 тестовых туров с реальными данными

### 5. Запустить сервер

```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Сервер будет доступен на `http://localhost:8000`

## API Документация

После запуска сервера документация доступна по адресам:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

## API Endpoints

### Health Check
```
GET /health
```

### Туры

#### Получить список туров
```
GET /api/v1/tours/
```

**Query параметры:**
- `page` (int) - номер страницы (по умолчанию: 1)
- `page_size` (int) - размер страницы (по умолчанию: 10, макс: 100)
- `country` (string) - фильтр по стране
- `min_price` (float) - минимальная цена
- `max_price` (float) - максимальная цена
- `start_date` (datetime) - начало периода
- `end_date` (datetime) - конец периода

**Пример:**
```bash
curl "http://localhost:8000/api/v1/tours/?country=Франция&min_price=500&max_price=1000"
```

#### Получить тур по ID
```
GET /api/v1/tours/{tour_id}
```

**Пример:**
```bash
curl "http://localhost:8000/api/v1/tours/1"
```

### Бронирование

#### Создать бронирование
```
POST /api/v1/bookings/
```

**Body:**
```json
{
  "tour_id": 1,
  "customer_name": "Иван Иванов",
  "customer_email": "ivan@example.com",
  "customer_phone": "+7 999 123 45 67",
  "number_of_people": 2,
  "notes": "Просьба забронировать номер с видом на море"
}
```

**Пример:**
```bash
curl -X POST "http://localhost:8000/api/v1/bookings/" \
  -H "Content-Type: application/json" \
  -d '{
    "tour_id": 1,
    "customer_name": "Иван Иванов",
    "customer_email": "ivan@example.com",
    "customer_phone": "+7 999 123 45 67",
    "number_of_people": 2
  }'
```

#### Получить бронирование по ID
```
GET /api/v1/bookings/{booking_id}
```

#### Получить бронирования по email
```
GET /api/v1/bookings/?email=ivan@example.com
```

## Структура проекта

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py              # FastAPI приложение и конфигурация
│   ├── config.py            # Настройки приложения
│   ├── database.py          # Подключение к БД
│   ├── models/              # SQLAlchemy модели
│   │   ├── __init__.py
│   │   └── tour.py          # Tour и Booking модели
│   ├── schemas/             # Pydantic схемы
│   │   ├── __init__.py
│   │   └── tour.py          # Схемы для валидации
│   ├── crud/                # CRUD операции
│   │   ├── __init__.py
│   │   └── tour.py          # Операции с турами и бронированиями
│   └── api/                 # API endpoints
│       └── v1/
│           ├── __init__.py
│           ├── tours.py     # Endpoints для туров
│           └── bookings.py  # Endpoints для бронирований
├── init_db.py               # Скрипт инициализации БД
├── requirements.txt         # Python зависимости
├── .env.example             # Пример конфигурации
├── .gitignore              # Git ignore файл
└── README.md               # Этот файл
```

## Модели данных

### Tour (Тур)
- `id` - уникальный идентификатор
- `title` - название тура
- `country` - страна
- `city` - город
- `description` - описание
- `price` - цена за человека
- `duration_days` - длительность в днях
- `max_people` - максимальное количество людей
- `available_slots` - доступные места
- `image_url` - URL изображения
- `start_date` - дата начала
- `end_date` - дата окончания
- `created_at` - дата создания
- `updated_at` - дата обновления

### Booking (Бронирование)
- `id` - уникальный идентификатор
- `tour_id` - ID тура
- `customer_name` - имя клиента
- `customer_email` - email клиента
- `customer_phone` - телефон клиента
- `number_of_people` - количество человек
- `total_price` - общая стоимость (рассчитывается автоматически)
- `booking_date` - дата бронирования
- `status` - статус (confirmed/cancelled)
- `notes` - заметки
- `created_at` - дата создания

## Тестовые данные

После запуска `init_db.py` будут созданы 10 туров в разные страны:
- 🇹🇷 Турция - Стамбул
- 🇫🇷 Франция - Париж
- 🇮🇹 Италия - Рим
- 🇪🇸 Испания - Барселона
- 🇳🇱 Нидерланды - Амстердам
- 🇨🇿 Чехия - Прага
- 🇦🇹 Австрия - Вена
- 🇬🇧 Великобритания - Лондон
- 🇬🇷 Греция - Афины
- 🇦🇪 ОАЭ - Дубай

## Разработка

### Добавление нового endpoint

1. Создайте функцию в соответствующем файле в `app/api/v1/`
2. Используйте type hints для параметров и возвращаемых значений
3. Используйте Pydantic схемы для валидации
4. Документация автоматически обновится в Swagger UI

### CORS настройки

По умолчанию разрешены запросы с:
- `http://localhost:3000` (React dev server)
- `http://localhost:5173` (Vite dev server)

Для изменения настроек CORS отредактируйте `app/config.py` или `.env` файл.

## Troubleshooting

### Ошибка "Database is locked"
SQLite поддерживает ограниченное количество одновременных записей. Это нормально для разработки. Для production используйте PostgreSQL.

### Ошибка импорта модулей
Убедитесь, что виртуальное окружение активировано:
```bash
# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### Порт 8000 занят
Запустите на другом порту:
```bash
uvicorn app.main:app --reload --port 8001
```

## Примечания

- ⚠️ Это учебный проект без реальной оплаты
- ⚠️ SQLite подходит для разработки, для production используйте PostgreSQL
- ⚠️ Не используйте в production без дополнительной настройки безопасности
