# Tour Booking Chatbot

AI чат-бот для туристического агентства с использованием LangChain и OpenAI.

## 🏗️ Архитектура

- **Main Agent**: Главный агент с поддержкой tools для вызова бэкенд API
- **Tools**: Инструменты для взаимодействия с бэкендом (обязательно используются)
- **Memory**: Память разговоров для сохранения контекста
- **Prompts**: Системные промпты для управления поведением агента

## ✅ Требования выполнены

- ✅ **Архитектура агента**: MainAgent с поддержкой tools
- ✅ **Промпты**: Системные промпты для управления поведением
- ✅ **Инструменты**: 5 инструментов для работы с бэкенд API (обязательно используются)
- ✅ **Память**: ConversationBufferMemory для сохранения контекста разговора
- ✅ **Использование tools**: Агент ОБЯЗАТЕЛЬНО использует tools для получения данных из бэкенда

## 🛠️ Технологии

- **Python 3.9+**
- **LangChain** - фреймворк для работы с LLM
- **OpenAI GPT-4** - языковая модель
- **Requests** - для HTTP запросов к бэкенду

## 📦 Установка

1. Создайте виртуальное окружение:
```bash
cd chatbot
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` из `.env.example`:
```bash
cp .env.example .env
```

4. Заполните переменные окружения в `.env`:
- `OPENAI_API_KEY` - ваш OpenAI API ключ
- `BACKEND_URL` - URL бэкенда (по умолчанию: http://localhost:8000)

## 🚀 Запуск

Перед запуском убедитесь, что бэкенд запущен на `http://localhost:8000`

```bash
python src/main.py
```

## 🔧 Инструменты (Tools)

Чат-бот использует следующие инструменты для работы с бэкендом:

1. **get_tours** - Поиск туров с фильтрацией
   - Параметры: country, min_price, max_price, start_date, end_date, page, page_size
   
2. **get_tour_details** - Детальная информация о туре
   - Параметры: tour_id
   
3. **create_booking** - Создание бронирования
   - Параметры: tour_id, customer_name, customer_email, customer_phone, number_of_people, notes
   
4. **get_booking_details** - Детали бронирования
   - Параметры: booking_id
   
5. **get_user_bookings** - История бронирований пользователя
   - Параметры: email

## 📁 Структура проекта

```
chatbot/
├── src/
│   ├── agent/
│   │   ├── __init__.py
│   │   └── main_agent.py      # Главный агент
│   ├── prompts/
│   │   ├── __init__.py
│   │   └── system_prompts.py  # Системные промпты
│   ├── tools/
│   │   ├── __init__.py
│   │   ├── backend_tools.py   # Инструменты для бэкенда
│   │   └── tool_registry.py   # Регистрация tools
│   ├── memory/
│   │   ├── __init__.py
│   │   └── conversation_memory.py  # Память разговоров
│   └── main.py                # Точка входа
├── .env                       # Переменные окружения (не в git)
├── .env.example               # Пример конфигурации
├── .gitignore
├── requirements.txt
└── README.md
```

## 🔌 Интеграция с бэкендом

Чат-бот работает с бэкендом через REST API:

- **Base URL**: `http://localhost:8000/api/v1`
- **Endpoints**:
  - `GET /tours/` - список туров
  - `GET /tours/{id}` - детали тура
  - `POST /bookings/` - создание бронирования
  - `GET /bookings/{id}` - детали бронирования
  - `GET /bookings/?email=...` - бронирования по email

## 💡 Примеры использования

### Поиск туров
```
Вы: Покажи туры во Францию
Бот: Ищу туры во Францию...
```

### Детали тура
```
Вы: Расскажи про тур с ID 1
Бот: Получаю информацию о туре...
```

### Бронирование
```
Вы: Хочу забронировать тур
Бот: Какой тур вас интересует? Мне нужны ваши данные...
```

### История бронирований
```
Вы: Покажи мои бронирования
Бот: Укажите ваш email для поиска бронирований...
```

## 🎯 Особенности

- ✅ Агент **обязательно** использует tools для получения данных
- ✅ Не выдумывает информацию о турах
- ✅ Сохраняет контекст разговора
- ✅ Обрабатывает ошибки бэкенда
- ✅ Поддерживает фильтрацию и поиск туров
- ✅ Помогает с бронированием туров

## 📝 Примечания

- Убедитесь, что бэкенд запущен перед запуском чат-бота
- API ключ OpenAI должен быть валидным
- Для production рекомендуется использовать более надежное хранилище памяти (Redis, PostgreSQL)


