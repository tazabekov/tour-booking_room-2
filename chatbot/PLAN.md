# План разработки чат-бота Tour Booking

## ✅ Выполнено

- [x] **Архитектура агента** - MainAgent с поддержкой tools через LangChain
- [x] **Промпты** - Системные промпты для управления поведением агента
- [x] **Инструменты** - 5 инструментов для работы с бэкенд API:
  - [x] get_tours - поиск туров с фильтрацией
  - [x] get_tour_details - детали тура
  - [x] create_booking - создание бронирования
  - [x] get_booking_details - детали бронирования
  - [x] get_user_bookings - история бронирований
- [x] **Память** - ConversationBufferMemory для сохранения контекста
- [x] **Использование tools** - Агент ОБЯЗАТЕЛЬНО использует tools для получения данных

## 📋 Структура проекта

```
chatbot/
├── src/
│   ├── agent/
│   │   └── main_agent.py      # Главный агент
│   ├── prompts/
│   │   └── system_prompts.py  # Промпты
│   ├── tools/
│   │   ├── backend_tools.py   # Инструменты для API
│   │   └── tool_registry.py   # Регистрация tools
│   ├── memory/
│   │   └── conversation_memory.py  # Память
│   └── main.py                # Точка входа
├── requirements.txt
├── .env.example
└── README.md
```

## 🔌 Интеграция с бэкендом

Бэкенд API endpoints:
- `GET /api/v1/tours/` - список туров (фильтры: country, min_price, max_price, start_date, end_date)
- `GET /api/v1/tours/{id}` - детали тура
- `POST /api/v1/bookings/` - создание бронирования
- `GET /api/v1/bookings/{id}` - детали бронирования
- `GET /api/v1/bookings/?email=...` - бронирования по email

## 🚀 Следующие шаги

1. Протестировать чат-бота с запущенным бэкендом
2. Добавить обработку ошибок бэкенда
3. Улучшить промпты на основе тестирования
4. Добавить логирование
5. Оптимизировать работу с памятью

## 📝 Заметки

- Агент использует LangChain с OpenAI Tools для обязательного вызова инструментов
- Все данные о турах получаются через API, не генерируются
- Память сохраняется в ConversationBufferMemory (в памяти процесса)
- Для production можно добавить Redis для распределенной памяти


